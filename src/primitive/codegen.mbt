///|
pub let parm0 : Array[String] = {
  let int = ["Byte", "Bytes", "Int", "Int64", "UInt", "UInt64"]
  let float = ["Float"]
  let base = ["Unit", "Bool", "String"]
  let all = []
  all.append(int)
  all.append(float)
  all.append(base)
  all
}

///|
pub let parm1 : Array[String] = {
  let all = ["FixedArray"]
  all
}
